logging:
  level:
    root: info
spring:
  application:
    name: myrpg
  profiles:
    active: local
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
