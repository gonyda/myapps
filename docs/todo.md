# MyRPG 개발 TODO

---

## 1. 버그 수정 및 안정성 개선

- (현재 등록된 버그 없음)

---

## 2. UI/UX 및 편의성 개선

- **스킬 목록 팝업 내 스킬 유형/종류(일반, 강, CC, 디버프 등) 뱃지 표기**:
  - 요구사항: 근접/궁술/마법 스킬 목록에서 각 스킬이 일반/강/CC/디버프/버프/방어/궁극기 중 어떤 메커니즘인지 직관적으로 파악하기 어려움.
  - 개선: `SkillRowView`에 `SkillType` 라벨/유형 정보를 바인딩하고, `skill-popup.html`의 스킬 이름/랭크 옆에 종류 뱃지(예: `[일반]`, `[강]`, `[CC]`, `[디버프]`, `[버프]`, `[방어]`, `[궁극기]`)를 시각적으로 추가.
- **게임 내 시간 시스템 (In-Game Time) (완료 - 2026-08-28)**:
  - 현실 시간 시계(`Clock`) 대신 가상 인게임 시간(00:00~23:59 24시간 순환, `inGameMinutes`) 캐릭터 진행 상태(`CharacterProgress`)에 영속화.
  - 필드 1노드 이동 시 +15분, 던전 1방 이동 시 +5분 경과 (실제 이동 성공 시에만 경과, Blocked 시 0분 불변).
  - 좌측 네비바 최하단에 24시간 디지털 시계 위젯 배치 및 인게임 시간 기반 배경/천체/상황 멘트 연동 완료.
- **인게임 시간대별 웹 배경화면 색상 동적 전환 (완료 - 2026-08-28)**:
  - 6대 시간대(`TimeOfDay`: 심야, 새벽, 오전, 오후, 황혼, 밤)에 맞춰 메인 스테이지(`.center`)에 딥 다크 스카이 앰비언스 그라디언트, 천체(해/달) 궤적 소프트 글로우, 미세 별빛 레이어 및 실시간 동기화 구현 완료.

---

## 3. 게임 내 환경 & 시스템 확장 (백로그)

- **밤 시간대 기습 확률 대폭 상향 (야간 위험도 증대)**:
  - 밤 시간대(20:00~06:00) 몬스터 기습(선공) 확률을 기존 5%에서 50%로 상향하여 야간 탐험의 긴장감 부여.
- **캠프파이어 & 야영 시스템 (Campfire & Rest)**:
  - 필드 노드에서 `[모닥불 피우기]` 상호작용으로 야영 설치.
  - 위험한 밤 시간 스킵(아침으로 진행), HP/MP/스태미나/부상 점진적 회복.
  - 모닥불 음식 쉐어링 버프 등 마비노기식 감성 연출.
- **보스 랜덤 등장 (필드)**:
  - 필드에 랜덤 시간 + 랜덤 위치로 보스 등장(스폰 설정 + 동적 배치 서비스 + 런타임 스폰 상태 관리).
