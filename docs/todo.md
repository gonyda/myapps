# MyRPG 개발 TODO

---

## 1. 전투 시스템 및 전략성 개선 (심리전 & 스킬 전략화)

- **전투 전략성 및 가위바위보 심리전 강화**:
  - **현상 및 문제점**:
    - 현재 일반 공격(베기), 디펜스 등을 무지성으로 반복 클릭하다가 우연히 승리하는 등 단조로운 전투 진행.
    - 전투 중 적의 행동을 예측하고 전략적으로 스킬을 선택하는 '머리 쓰는 재미' 부족.
  - **개선 목표**:
    - 가위바위보 상성(일반/강공격/디펜스/카운터)과 턴 메카닉을 고도화하여, 플레이어가 전략과 심리전을 정교하게 구사하면 **1레벨 캐릭터도 상위 몬스터를 힘겹게라도 공략할 수 있는 전략적 여지(플레이어 기량 개입)** 마련.
  - **검토/작업 항목**:
    - 몬스터 행동 패턴/전조 힌트(예: 준비 동작 텍스트, 턴별 AI 심리전) 강화.
    - 상성 성공/실패 시의 리턴과 리스크(경직, 스태미나/마나 소모 대비 기대치, 반격 타이밍) 밸런스 재설계.

---

## 2. [완료] 맵 & 몬스터 데이터 조정 및 신규 추가

- **몬스터 명칭 변경**:
  - `거미` $\rightarrow$ `흰 거미`로 명칭 변경 (`monster.json` 및 관련 참조).
- **신규 맵 노드 추가**:
  - **맵 이름**: `공동 묘지` (`graveyard`)
  - **위치/연결**: `동쪽 언덕` 북쪽에 연결 노드로 배치 (x: 1, y: -1).
  - **출현 몬스터**: `흰 거미` (`spider`)
- **신규 몬스터 추가**:
  - **몬스터 이름**: `붉은 여우` (`red-fox`)
  - **스폰 위치**: `동쪽 언덕` (`east-hill`)
  - **설정 스탯**: 2레벨 기준 (HP 44, ATK 38, DEF 2, EXP 20, Gold 5~13), CP 검증 시 난이도 계수 **'쉬움(easy, CP 41.5 / 난이도비 0.81)'** 적용 (너구리와 흰 거미 사이 위치).
- **기존 몬스터 밸런스 하향 (전투력 조정)**:
  - `너구리` (HP 38, ATK 36, DEF 2, CP 37.5 / 난이도비 0.85), `흰 거미` (HP 50, ATK 40, DEF 2, CP 45.4 / 난이도비 0.88) 하향 조정.
  - `verify_monster.py` CP 검증 시 **'쉬움(easy)'** 기준 밸런스 검증 완료.

---

## 3. UI/UX 및 웹 환경 개선

- **스킬 사용 시 alert 팝업창 제거**:
  - 스킬 시전 시 불필요하게 뜨는 alert 알림창을 제거하여 턴 진행의 번거로움 해소 및 쾌적하고 빠른 전투 템포 확보.
- **인게임 시간대별 웹 배경화면 색상 동적 전환**:
  - 현재 세팅된 인게임 시간대(`TimeOfDay`: 새벽, 아침, 낮, 노을, 밤, 심야)에 따라 웹 전체 배경화면 색상이 동적으로 변경되도록 구현.
  - 눈에 피로를 주지 않는 **은은하고 자연스러운 테마/배경색** 연출.

---

## 4. [완료] 테스트 & 디버깅 편의 기능 (치트 버튼)

- **테스트용 경험치 획득 버튼 (`+1K EXP`)**:
  - 좌측 네비바에 버튼 배치 (`.btn-cheat`).
  - 클릭 시 `POST /cheat/exp` 호출 $\rightarrow$ 즉시 **고정 1,000 EXP** 획득 및 레벨업/AP/풀회복 반영.
- **테스트용 골드 획득 버튼 (`+1K Gold`)**:
  - 좌측 네비바에 버튼 배치 (`.btn-cheat`).
  - 클릭 시 `POST /cheat/gold` 호출 $\rightarrow$ 즉시 **고정 1,000 Gold** 획득 및 상단바/정보창 동기화.
- *(임시 개발 편의 도구이며 추후 필요 시 삭제 예정)*

---

## 5. 계정 및 인증 시스템

- **간이 로그인 기능 (로그인 화면 우선 구현)**:
  - 회원가입 절차 없이 **로그인 화면 UI** 및 기본 인증 처리 구현.
  - 관리자가 DB 유저 테이블에 직접 계정(Row)을 추가하여 관리 및 로그인 가능하도록 연동.

---

## 6. 개발 도구 & AI 워크플로우 (Skills)

- **[완료] 게임 데이터 추가 가이드 Skill (`skills/myrpg-data-balance/SKILL.md`)**:
  - `skills/myrpg-data-balance/SKILL.md` (SSOT), `.agents/skills/myrpg-data-balance/SKILL.md`, `.cline/skills/myrpg-data-balance/SKILL.md` 생성 완료.
  - 기존 `rules/myrpg/data-balance-guide.md` 삭제 및 스킬 내부로 통합.
  - 아이템, 몬스터, 스킬, 맵 추가 시 에이전트와 사용자가 대화형(Q&A)으로 밸런스를 검증하고 `tools/balance/` 파이썬 스크립트로 자동 검수한 뒤 JSON을 안전하게 작성하는 워크플로우 구축 완료.

---

## 7. 게임 내 환경 & 시스템 확장 (기존 백로그)

- **게임 내 시간 시스템 (In-Game Time)**:
  - 현실 시간 시계(`Clock`) 대신 가상 인게임 시간(Day N, 00:00~23:59) 캐릭터 진행 상태(`CharacterProgress`)에 영속화.
  - 맵 이동 시 일정 시간 소모 (예: 30분~1시간), 전투 중에는 정지.
- **밤 시간대 기습 확률 대폭 상향 (야간 위험도 증대)**:
  - 밤 시간대(20:00~06:00) 몬스터 기습(선공) 확률을 기존 5%에서 30%로 상향하여 야간 탐험의 긴장감 부여.
- **캠프파이어 & 야영 시스템 (Campfire & Rest)**:
  - 필드 노드에서 `[모닥불 피우기]` 상호작용으로 야영 설치.
  - 위험한 밤 시간 스킵(아침으로 진행), HP/MP/스태미나/부상 점진적 회복.
  - 모닥불 음식 쉐어링 버프 등 마비노기식 감성 연출.
- **보스 랜덤 등장 (필드)**:
  - 필드에 랜덤 시간 + 랜덤 위치로 보스 등장(스폰 설정 + 동적 배치 서비스 + 런타임 스폰 상태 관리).